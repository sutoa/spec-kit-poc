# Frontend Application (React with Vite)

This directory contains the React frontend application for the Account Viewer.

## Setup

1.  **Install dependencies:**
    ```bash
    npm install
    ```

2.  **Configure environment variables:**
    Create a `.env` file in the `frontend/` directory if you need to override the API base URL.
    ```
    VITE_API_BASE_URL=http://localhost:8000
    ```

## Running the Application

1.  **Ensure the backend is running.**
2.  **Start the frontend development server:**
    ```bash
    npm run dev
    ```
    The application will be available at `http://localhost:5173`.

## Running Tests

(No frontend tests implemented yet.)
